// Navigation routes were added during an experiment and removed.
// If you need a navigation API in the future, recreate this file
// and implement GET /api/navigation with proper permission checks.

module.exports = {}
